# 開發紀錄

這份是工作階段的重點整理，記錄「為什麼這樣做」與「踩過的坑」——
程式碼看得出來的事（結構、命名、有哪些檔案）不寫在這裡，看 code 就好。

---

## 一、日期欄位：為什麼要自己寫日曆

### 問題

原生 `<input type="date">` 點下去時，年／月／日那一段會出現**系統藍底的選取highlight**，
跟專案配色完全不搭。

### 結論：CSS 改不掉，不要再試

那個藍底是 Chrome 畫在 input 的 **shadow DOM 內部**的 selection highlight，
而 **highlight 樣式不會繼承進 shadow DOM**。所以下面這些全部無效：

| 試過的做法                                 | 為什麼失敗                                          |
| ------------------------------------------ | --------------------------------------------------- |
| `::-webkit-datetime-edit-year-field:focus` | 那些欄位**不會**拿到 focus，focus 在 `<input>` 本身 |
| `::selection`                              | highlight 樣式不繼承進 shadow DOM                   |
| `::-webkit-datetime-edit-*` 改背景         | 同上，碰不到 highlight                              |
| 用 `<label>` 包住 input                    | 跟 shadow DOM 無關，沒有幫助                        |
| 把規則從 `<style scoped>` 搬到全域         | 解決了 scoped 的問題，但**根本原因不是 scoped**     |

**不要再往 `main.css` 或 `Date.vue` 加 CSS 試這件事。**

### 附帶發現：`<style scoped>` 碰不到 shadow DOM

Vue 的 `<style scoped>` 會把選擇器編譯成屬性選擇器：

```css
/* 你寫的 */
.form-date input[type='date']::-webkit-calendar-picker-indicator { display: none }

/* 實際編譯出來的 */
.form-date input[type='date'][data-v-847f62fb]::-webkit-calendar-picker-indicator { ... }
```

shadow DOM 內部的節點**不帶 `data-v-` 屬性**，所以規則完全失效。

因為這樣，`::-webkit-calendar-picker-indicator { display: none }` 這條原本寫在
`Date.vue` 的 scoped 區塊裡，**一直都是死的**——原生日曆圖示從來沒被藏起來過。
已搬到 `assets/styles/main.css` 的 `@layer components` 才真正生效。

> 驗證方法：用 `@vue/compiler-sfc` 的 `compileStyle()` 直接把 SFC 的 style 編譯出來看，
> 不要用猜的。也可以直接翻 `.nuxt/dist/client/_nuxt/` 裡 build 出來的 CSS。

### 最後的做法

`components/UI/Form/Date.vue` 改成**桌機自製日曆 + 觸控裝置維持原生 input**：

- 判斷方式 `window.matchMedia('(pointer: coarse)')`，**必須在 `onMounted` 裡做**，
  寫在 ref 初始值會造成 hydration mismatch。
- 桌機走自製日曆，藍底問題自然消失。
- 手機維持原生——原生的日期滾輪比任何自製日曆好用，而且藍底是桌機才有的症狀。
- 版面邏輯（定位、Teleport、外點關閉、鍵盤操作）**照抄 `components/UI/Form/MultiSelect.vue`**，
  沒有從零寫。專案沒有裝任何日期套件，這是唯一的路。

### 兩個容易踩的雷

1. **日期一律用本地時間組字串**，不要用 `toISOString()`——那是 UTC，會整天偏掉。
   要用 `getFullYear()` / `getMonth()` / `getDate()` 自己拼。
2. **彈出面板一定要 `Teleport to="body"`**。`<main>` 是專案唯一的捲動容器
   （`html` / `body` 在 main.css 被設成 `overflow: hidden`），不 teleport 會被裁掉。
3. 日曆格子固定畫 **42 格**（6 週），面板高度才不會隨月份跳動。

---

## 二、詢價單的欄位決策

### 聯絡資訊

畫面上是兩欄流動排列（每欄 `md="6"`），順序：

```
┌─────────────────────┬─────────────────────┐
│ 1 聯絡人姓名 *      │ 2 聯絡電話 *        │
├─────────────────────┼─────────────────────┤
│ 3 公司名稱          │ 4 公司統編          │
├─────────────────────┼─────────────────────┤
│ 5 電子郵件 (Email)  │ 6 希望交期          │
└─────────────────────┴─────────────────────┘
```

剛好六格填滿三列，不會空半格。手機寬度變單欄，由上而下 1→6。

### 驗證一律「寬鬆」，這是刻意的

擋太嚴會擋到真客戶，所以只做最低限度的防呆：

| 欄位               | 必填 | 規則                                                                                |
| ------------------ | ---- | ----------------------------------------------------------------------------------- |
| 聯絡人姓名         | ✅   | 非空白                                                                              |
| 聯絡電話           | ✅   | 去掉非數字後**至少 8 碼**。`02-1234-5678`、`0912345678`、`04-22334455#123` 都要能過 |
| 公司統編           | ❌   | 填了才驗，**8 位數字**。刻意**不做**官方檢查碼演算法                                |
| Email              | ❌   | 填了才驗，有 `@`、`@` 後面有點、都不在頭尾                                          |
| 公司名稱／送貨地點 | ❌   | 不驗                                                                                |

`showErrors` 預設 `false`，按下「確認詢價」才轉 `true`——
一進頁面就滿江紅很難看。

> 統編或 Email 格式錯誤也會擋下送出，所以「請補齊聯絡資訊」那個
> 提示視窗的文字要一併提到格式，否則客戶會以為是姓名電話有問題。

### 其他決定

- **聯絡資訊不進 CSV**。CSV 維持只有項目資料：
  `項次,品項,選擇,規格,數量,參考圖檔,備註`
  圖面也不進 CSV（無論是 SVG 或 PNG 路徑），要看圖請匯出 PDF。
- **送出後不清空聯絡資訊**。demo 時常要連送好幾次，每次重打很煩。
- **參考圖檔只記檔名**，檔案本身不上傳（沒有後端）。
- 詢價單表格**刻意不顯示**參考圖檔與備註兩欄——客戶自己填的時候就看過了，
  但 CSV 要留著，老闆報價時才看得到特殊要求。
- `useQuoteContact` 跟 `useQuoteCart` **分開**：一個是訂單層級、一個是清單，
  生命週期不同（清空清單不該清掉聯絡資訊）。

### 已移除的東西

**「相關選購項目」已從詢價單移除**（同事反應放在購物車沒意義）。

`components/Product/RelatedList.vue` 本身**留著沒刪**，目前沒有任何地方引用。
連帶地 `useAppNavigation.ts` 的 `comingSoon` 欄位也暫時沒作用——
那個欄位當初就是為了這個元件加的。想放到品項頁的話隨時能接回去。

---

## 三、Notion 對照的規矩

**Notion 沒提到的欄位不要自己加，Notion 標問號的地方不要自己填。**

已經取得授權的例外（這些可以做）：
數量、參考圖、備註、交期、聯絡資訊、送貨地點。

所以像「數值範圍檢查」這種 Notion 打問號的東西，程式裡就只做防呆、不做範圍驗證。

### 用字：圖示 vs 圖

客戶看得到的地方，**檔案上傳一律叫「參考圖」或「參考圖檔」，不要寫「參考圖示」**——
「圖示」在這個專案是 icon 的意思（`UIIcon`、`components/UI/Steel/Icon.vue`、
sidebar 的「鋼材圖示」都是），客戶會誤會成要傳 icon。

反過來說，**要改這個字時不能全域取代**：`CLAUDE.md`、`/UI/*` 展示頁、`useAppNavigation.ts`
裡的「圖示」全都真的是 icon，動到就錯了。要一個一個看。

---

## 四、扁鐵詢價頁（`components/Product/FlatBarForm.vue`）

> 填寫流程已從 `pages/products/flat-bar.vue` 搬進元件（見第六節），
> 下面講的欄位與圖面邏輯全部在那個元件裡。頁面只剩標題與導頁。

四個步驟：**尺寸與規格 › 數量與長度 › 加工方式 › 圖解說明**。

### 哪些數字是暫填的（⚠️ 要問業主）

Notion 只寫「常用尺寸範圍：6分 ~ 5"」，**中間階級沒列**。
現在的七張尺寸卡（6分、7分、1"、2"、3"、4"、5"）裡，
**中間五階是為了 demo 有東西可點自己補的**，厚度也全部沿用範例值 4.5 m/m。

面寬用 Notion 自己給的換算率算（Notion 寫「面寬 3" 等於 7.5 cm」，即 1" = 2.5 cm），
這個有依據。分是八分之一吋，所以 6分 = 6/8"。

長度 300 cm、數量 20 支目前**寫死不能改**——但這兩個數字在 Notion 上是寫在
「**範例**」標題底下的，跟「厚度 4.5 m/m」同一組，也可能只是舉例。

「加工方式」步驟目前**只有「客製沖孔」一張卡**（Notion 扁鐵段只出現這四個字），
一個選項的步驟其實沒有選擇意義。「不沖孔／正常孔／客製孔」的三選一是
**C 型鋼**那段才有的，沒有套到扁鐵。

### 孔位的三個欄位為什麼會互相連動

欄位是「兩端邊緣各留」「孔數」「間距」，三個數字裡只有兩個是自由的
（Notion 的算式 `5 + 72.5×4 + 5 = 300cm` 綁死總長），所以改一格另一格自動重算：

| 客戶改動 | 自動重算             |
| -------- | -------------------- |
| 間距     | 兩端邊緣             |
| 兩端邊緣 | 間距                 |
| 孔數     | 兩端邊緣（保留間距） |

> 副作用：客戶填的數字**會被系統改掉**。堅持要「邊緣 5、間距 51」時系統會把邊緣改成 48，
> 因為兩者不可能同時成立。這是三個數字連動的必然結果，不是 bug。

**只做一個邊緣欄位、一個間距欄位**是刻意的——填不出「左邊 5、右邊 8」這種不對稱的組合。

### 圖只畫合法結果

數字湊不滿總長時，圖**不照著硬畫**，改畫依孔數自動置中的排法，並在圖下註明。
這是為了修掉實際發生過的問題：填 4 孔／邊緣 5／間距 72.5 時，
圖會畫成左邊 5cm、右邊 77.5cm——圖在示範一個規則不允許的結果。

### 誤差丟給間距，不丟給邊緣

欄位只能填到小數第一位，連動算出來的值不一定剛好是一位小數，
所以總得有一格讓步。**圖固定讓間距讓步，兩端邊緣一律照客戶填的顯示**，
因為客戶剛動的就是邊緣那一格，把他剛打的數字改掉最違反直覺。

代價：標註會出現小數第二位（例如 72.75cm），但欄位裡是 72.8cm，兩者不完全一致。
誤差不會消失，只能決定丟到哪一格。

已用 3630 組數字驗算過：通過驗證的組合，頭距一定等於尾距、每段間距完全相同、
孔群中心落在總長正中間。

### 已知限制（可接受，不用修）

- 孔數 8 以上時，兩端約 27.6 單位的線段塞不下「5cm」的字。
- 孔數 4/7/8/9/10/12 以上時，標註四捨五入後的加總跟 300 差最多 0.14cm。
- 12 孔時標註文字會互相重疊。

### 沒做、程式碼裡留成註解的

- 孔徑欄位（Notion 原圖只畫圓圈、沒標孔徑）
- 「等距均分／指定端距與節距」的排列方式切換（Notion 只描述一種排法）

兩個都保留成註解，要開放隨時可以復原。

---

## 五、詢價單的圖面與匯出 PDF

### 圖面存快照，不存參數

按下「加入詢價」的當下，把圖面存進 `QuoteCartItem`，之後不再重算。
這是刻意的語意：**客戶按下加入那一刻看到什麼，詢價單就顯示什麼。**

存快照而不是存孔位參數，是為了讓詢價單不必知道各品項頁怎麼畫圖——
每個品項的圖都不一樣，存參數的話 `QuoteCartItem` 就得為每種品項各留一組欄位。

### 兩個圖面欄位，不要合併

| 欄位            | 內容                       | 誰在用   |
| --------------- | -------------------------- | -------- |
| `diagramSvg`    | 序列化的 `<svg>` 標記字串  | 扁鐵     |
| `diagramImages` | `{ src, caption }[]`，選填 | 鋼板彎折 |

**鋼板彎折沒有 SVG。** 它的圖是 Notion 原圖裁切出來的固定 PNG
（`public/images/products/bending/*.png`），跟扁鐵那種依輸入即時運算的向量圖
是兩回事。所以是存檔案路徑，不是抓 svg。

**不要把 `<img>` 字串塞進 `diagramSvg` 來省一個欄位。** 三個理由：

1. 破壞該欄「內容是 svg」的契約（註解與 `v-html` 的安全理由都綁在這上面）
2. `[&>svg]:w-full` 這類定尺寸的選擇器對 `<img>` 完全無效，圖會用原始像素爆版
3. 程式無法列舉出「有哪些圖要等載入」，下面那個修法直接做不到

`diagramImages` 是**選填**的，消費端一律用 `item.diagramImages?.length` 判斷。
這樣只有 svg 的品項頁不必為了它多傳一個空陣列。

### ⚠️ 列印前一定要等圖片載完

`window.print()` 之前只做 `await nextTick()` **是不夠的**，會印出空白的圖框。

三件事湊在一起造成的：

- `nextTick()` 只保證 Vue 把 DOM 更新完，**不等 `<img>` 下載**
- 列印版面平常是 `display: none`，瀏覽器對隱藏子樹裡的圖會延後甚至不載
- `window.print()` 是**同步阻塞**的，不會等圖

解法是列印前用 `img.decode()` 逐張等到解碼完（見 `quote-cart.vue` 的
`waitForPrintImages()`）。用 `decode()` 而不是 `onload`：已載好的圖立即 resolve，
不必自己寫 `complete ? ... : addEventListener` 那套樣板。任何一張失敗都不擋列印。

> 扁鐵沒踩到這個雷，因為 SVG 是內嵌標記、沒有網路請求。**PNG 有。**
> 所以這個雷是加了鋼板彎折的 PNG 才出現的，之前的版本看起來正常不代表沒問題。

### ⚠️ `overflow: hidden` 會讓 PDF 只有一頁

`@layer base` 把 `html` / `body` 設成 `overflow: hidden`（本專案的捲動容器是
`<main>` 不是 window）。那個設定在列印時會讓瀏覽器**只印出第一個畫面的高度**，
後面的頁全部被裁掉——症狀是「明明有五項，PDF 只有一頁」。

`main.css` 的 `@media print` 裡解除 `html` / `body` / `#__nuxt` / `#__nuxt > div` /
`main` 的高度與 overflow 限制，**那段不要拿掉**。

### 為什麼走瀏覽器列印，不用套件

`jspdf` + `html2canvas` 會把整頁轉成點陣圖：孔位圖的細線與標註數字會糊掉、
文字不能選取、中文還要另外塞字型。走列印的話 SVG 是向量的、字是真的字，
而且不必裝任何套件。

**代價：檔名由瀏覽器決定**（通常是頁面標題），`window.print()` 無法指定。
要 `詢價單_Q20260908-123.pdf` 那種檔名只能改用套件方案，代價是圖會糊。

### PDF 版面：跟螢幕刻意長得不一樣

螢幕上的三個區塊是為了**編輯**排的，PDF 是給老闆**讀**的，所以結構不同：

```
封面（只有一項時省略，總表只有一列跟後面那頁重複）
  詢價清單總表 + 交期與聯絡資訊
每項一頁
  圖面 → 項目明細（直式）→ 交期與聯絡資訊（每頁重複）
```

- **每頁重複印聯絡資訊**是刻意的：老闆可以把單頁撕下來給師傅，不必附別頁。
- 第二區用**直式的「項目明細」**而不是清單表格——一項只有一列的話，
  表頭會比內容還長，就失去「清單」的意義了。
- 參考圖檔與備註在螢幕表格上刻意不顯示，但 **PDF 要印**（跟 CSV 同理）。

### 圖面高度上限是算出來的，不是喬出來的

A4 扣掉 12mm 邊界剩 273mm。固定佔用約 146mm（頁首 22 + 項次小標 9 +
項目明細 58 + 交期聯絡 57），所以圖面區總高要 ≤ 105mm：

- 兩張並排 `max-h-[55mm]`
- 單張 `max-h-[70mm]`

**限高不限寬**：每張圖的長寬比都不一樣，限寬的話直式的圖會太高。
改版面（例如聯絡欄位增減）時這個預算要重算，不然項目明細會被擠到第二頁。

### 單號要先存起來

`makeOrderNo()` 每次呼叫都給不同的隨機碼。列印前先產生一次存進 ref，
整份 PDF 的每一頁頁首才會是同一個單號，否則每頁都不一樣。

---

## 六、詢價項目的修改與勾選送出

### `source`：存快照，也存參數

第五節說「圖面存快照，不存參數」。加了「修改」之後兩者並存，但**不衝突**：

| 欄位                           | 誰在讀           | 什麼時候讀         |
| ------------------------------ | ---------------- | ------------------ |
| `diagramSvg` / `diagramImages` | 表格、彈窗、PDF  | 每次顯示           |
| `source`                       | 只有「修改」彈窗 | 使用者主動按修改時 |

**`source` 不是拿來重算圖的**，顯示一律讀快照。它唯一的用途是把填寫流程還原成
使用者原本填的樣子——沒有它，編輯彈窗開起來會是空白表單。

`detail` / `summary` / `diagramSvg` 全部由 `source.state` 算出來，是**單向**的。
保證機制是「整包重算」：表單元件一律 emit 完整的 `Omit<QuoteCartItem,'id'>`，
呼叫端只能 `addItem` 或 `updateItem`，**沒有任何路徑可以只改其中一欄**。

`source` 是**選填**的。五個待補的品項頁還沒有表單元件，沒有 `source` 的項目
就不顯示「修改」鈕，其餘功能不受影響（比照 `diagramImages?` 的約定）。
新品項頁一律要帶——選填是為了讓還沒接編輯器的品項能先上線，不是可以省略。

**上傳的檔案不進 `source`**：`File` 物件無法序列化。編輯時只顯示原本的檔名，
沒重選就沿用——使用者只是回來改孔數，不該因為沒重傳而把附件記錄清掉。

### 填寫流程抽成元件

`components/Product/FlatBarForm.vue` 與 `BendingForm.vue`，
品項頁與編輯彈窗共用同一份。複製第二份的話兩邊遲早會走鐘。

`mode` prop 分兩種：

|          | `create`（品項頁）   | `edit`（彈窗）     |
| -------- | -------------------- | ------------------ |
| 步驟解鎖 | 前面填完才能點下一步 | **每一步都能點**   |
| 進來停在 | 第一步               | **最後一步**       |
| 送出後   | 清空欄位回第一步     | 不清（彈窗直接關） |
| 送出鈕   | 加入詢價 + 送出詢價  | 只有「儲存修改」   |

> ⚠️ **`goToStep` 的捲動容器要看模式。** 品項頁捲 `<main>`（專案唯一的捲動容器），
> 但彈窗是 Teleport 到 body 的 `UIModal`，捲動容器是 modal 內層的
> `.overflow-y-auto`。在彈窗裡捲 `<main>` 不會報錯，只是**捲到背景那一頁去了**。
> 兩個元件都用 `root.closest('.overflow-y-auto')` 處理。

`captureDiagram()` **不用改**：座標全是 `PLATE_*` 常數、`viewBox` 固定，
`outerHTML` 序列化的是標記不是渲染尺寸，彈窗裡 SVG 寬度不同不影響結果。

### 勾選模型（= 購物車的勾選結帳）

> 曾經有過一版 `composables/useInstantQuote.ts`，讓「送出詢價」把那一筆存在
> 清單之外的獨立暫存區。**已經整包刪掉**，看到舊資料提到它的話那是過時的。
> 改掉的原因是查證後發現不符合蝦皮的實際行為（見下面「送出詢價」）。

清單與勾選是兩件事：

- `items`（`quote-cart-items`）＝ **我收集了什麼**
- `selectedIds`（`quote-selected-ids`）＝ **這次要送出哪些**

送出、匯出 CSV、匯出 PDF 三者的範圍一律是 `selectedItems`，
表格則列出全部 `items`。東西都留著，只送打了勾的。

**勾選跟清單放同一個 composable**（`useQuoteCart`），這跟第二節
「`useQuoteContact` 要分開」的判準不衝突——判準是生命週期：
聯絡資訊是訂單層級、跟清單無關，勾選則是**完全綁在清單上**
（刪項目要清它的勾、清空要清全部）。分開的話這些同步得靠每個呼叫端自己記得做。

存 id 不存整筆：項目被「修改」時 id 不變，勾選就自動跟著。

> ⚠️ **`removeItem` 一定要一起清 `selectedIds`。** 留下指向不存在項目的殘留 id，
> `selectedItems` 用 `filter` 是不會顯示它，但 `isAllSelected` 的
> 「勾選數 === 項目數」會**永遠不成立**，全選框從此卡在半勾。

| 函式         | 對 `selectedIds` 要做的事      |
| ------------ | ------------------------------ |
| `addItem`    | 推進去（加入的預設就是要詢的） |
| `removeItem` | **移除該 id**                  |
| `clearItems` | 清空                           |
| `updateItem` | 不用動（id 不變）              |

`addItem` 回傳新 id，`selectOnly(addItem(payload))` 才串得起來。

### 送出詢價（= 蝦皮的「直接購買」）

`selectOnly(addItem(payload))` + 導到 `/quote-cart`。

語意是**加進清單 + 只勾這一項**，不是「不進清單」。
[蝦皮台灣幫助中心](https://help.shopee.tw/portal/4/article/137037)原文：
「如欲直接購買商品，亦可點選【直接購買】按鈕**進入購物車畫面**。」
（新加坡站的英文說明寫的是直接跳結帳，兩邊版本不同，以台灣的為準。）

這樣客戶看得到自己原本收集的項目都還在（只是沒打勾），
離開頁面再回來那一筆也還留著——比獨立暫存少一條「東西不見了」的誤會路徑。

### 勾選的 UI 約定

`UIFormCheckbox` 有選填的 `indeterminate` prop（給「全選」用）。
方框底色走 `isMarked` computed 而**不是 `peer-checked:`**：
半勾時 input 的 `checked` 是 `false`，純 CSS 選擇器選不到，
方框會變成白底配一條看不見的白線。

> ⚠️ 全選框與每一列都必須寫 `:model-value` + `@update:model-value`。
> `isAllSelected` / `isSelected()` 是 computed 與函式，`v-model` 會試著寫進去而報錯。

> ⚠️ 連帶地，`Checkbox.vue` **不可以用 `defineModel`**，
> 必須是 `props.modelValue` + `emit('update:modelValue', ...)`。
>
> `defineModel` 會在元件內部留一份本地值。呼叫端傳
> `:model-value` + 自己的 handler（而不是 `v-model`）時，點一下就會把
> input 自己的 `checked` 寫進那份本地值，`model` 從此跟 prop 脫鉤。
> 改成直接讀 prop、寫 emit 就完全不留本地狀態，兩種用法都正確。
>
> （註：這是理論上的隱患，`v-model` 那條路實際上一直是通的，
> 不是下面那個 bug 的成因。改了是因為它本來就該這樣寫。）

> ⚠️ 未選取的方框**不要加淡色的提示勾**（曾經加過 `hintCheck` prop，已移除）。
> 本意是要讓沒有文字標籤的勾選欄看起來可以點，
> 實際結果是使用者把淡灰色的提示勾看成真的勾，
> 明明沒選卻以為選了，反而讓人覺得「已勾選 0 項」是 bug。
> 空框就是空框，這是 checkbox 唯一不會被誤讀的狀態。

> ⚠️ 方框的背景色（`bg-white` / `bg-brand-500` / `bg-nurse-100`）
> **必須全部寫在同一條三元式裡**，不可以把 `bg-white` 拉回固定的 `class`。
> 這是 Tailwind v4 的樣式表順序問題，詳見**第七節**——
> 拆開寫的話打勾永遠不會變藍。

沒勾的列給 `opacity-55`：長清單裡光靠一個小方框太不明顯，客戶會沒發現漏勾。

一項都沒勾時三顆按鈕都 `disabled`，且**表格下方要寫出原因**——
只變灰不說明的話客戶只會覺得按了沒反應。程式面仍要擋（`hasSelection` 檢查），
`disabled` 只是視覺層。

`exportCsv(rows)` 是**必填參數**。給預設值的話忘記傳參會靜默匯出整個清單
而不是勾選的那些——必填則是編譯期就報錯。

右上角徽章維持顯示**清單總數**，不是勾選數：那顆按鈕代表「我收集了幾項」。

### 表格的參考圖檔與備註

第二節寫「刻意不顯示」，現在改成顯示，這是**刻意推翻**：
客戶送出前要確認「我剛剛傳的檔有沒有跟著送出」。

- 參考圖檔只顯示**個數**（迴紋針 icon + 「N 個」），完整檔名放 `title`。
  手機拍的檔名常超過 30 字，列出來會把規格欄撐爆。
- 備註**併在規格欄下方**，不另開一欄——表格已經七欄，再加一欄手機一定要橫向捲。
- 備註做**點擊展開**，不只靠 `title`：那個 tooltip 在觸控裝置上根本不會出現。
- `<table class="!min-w-[720px]">` 行內覆寫，**不動全站共用的 `UITable`**。

---

## 七、Tailwind v4：同性質的 class 不可以一半寫死、一半動態

**踩過的坑（實際發生在 `Checkbox.vue`）：**
方框把 `bg-white` 寫在固定的 `class` 屬性裡，選取時的 `bg-brand-500` 寫在 `:class` 三元式。
結果**打勾永遠不會變藍**。

原因是**兩者特異性完全相同**，誰贏由 **Tailwind 產出的樣式表順序**決定，
跟寫在 class 字串裡的前後**完全無關**（Prettier 的 class 排序也改變不了）。
v4 把 `bg-white` 排在 `bg-brand-500` 後面，所以白色永遠贏。

### 規則

**同一個 CSS 屬性的所有 class 要放進同一條三元式，互斥地產出。**

```vue
<!-- ✅ 對：背景色只由一條三元式決定 -->
<span
  class="grid size-5 rounded-[5px] border-[1.5px] text-white"
  :class="[disabled ? 'bg-nurse-100' : isMarked ? 'bg-brand-500' : 'bg-white']"
/>

<!-- ❌ 錯：bg-white 寫死在上面，動態的那個永遠贏不了 -->
<span class="bg-white ..." :class="[isMarked ? 'bg-brand-500' : '']" />
```

固定 `class` 只放**不會被覆寫**的東西（尺寸、圓角、`display`、`transition`）。

### 排查方法

**比對「DOM 的 class 字串」跟「`getComputedStyle` 的實際值」。**
class 裡明明有 `bg-brand-500`、算出來卻是 `rgb(255,255,255)`
→ 就是 CSS 優先序，**不是資料流**。

當時的症狀非常像資料錯誤：勾一項時「已勾選 1 項」正確、整列沒淡化、
三顆按鈕都亮、匯出內容也對，**就只有方框不變藍**。
因為白勾（`text-white`）畫在白底上，`<svg>` 其實一直都渲染著、只是看不見。
先查了選取邏輯、`useState` key 衝突、`UITable` 覆寫、主題 token，全部是對的——
**光讀程式碼看不出來，一定要看 `getComputedStyle`。**

> 排查過程的教訓：使用者說「互動邏輯沒錯、只有打勾沒顯示」時，
> 那句話就已經把範圍縮到**渲染層**了，不該再回頭查資料流。
> 診斷 code 要自己寫進頁面（暫時的 `onMounted` + `console.log`）讓使用者跑，
> 不要丟 console 一行指令請使用者貼——瀏覽器會擋貼上，而且錯了看不出來。

---

## 八、專案慣例備忘

> 這節只記容易忘或容易踩雷的，完整規範看 `CLAUDE.md`。

- **`npm run dev` 由使用者自己跑，Claude 不要跑**（也不要用 `timeout`、背景執行等變通）。
- npm scripts 一律寫 `node ./node_modules/nuxt/bin/nuxt.mjs`，不要用 `nuxt` binary（Windows 相容性）。
- `useAppNavigation.ts` 的 `mainNavigation` 是 **append-only**：
  `homeItem` / `uiWorkspace` 硬編碼在 `[0]` / `[1]`，**重排陣列會壞掉**。
  只能在既有物件上加欄位。
- 表單外觀改 `composables/useFormControlStyles.ts`，不要逐一改元件。
- Tailwind v4 沒有 `tailwind.config`，token 全在 `assets/styles/main.css` 的 `@theme static`。
- 元件名 = `components/` 底下的路徑，沒有 prefix。移動檔案等同改名，全站引用都要跟著改。
- `UIFieldGroup` 的欄位數是奇數時要補一格白的，否則 `bg-nurse-200` + `gap-px` 的格線底色會露出來。
- 數量欄位清空時 model 是**空字串**，`'3' >= 1` 會因強制轉型成立，必須先卡 `typeof === 'number'`。
- 上傳與備註要放在 `UIFieldGroup` **外面**——這兩個比一般欄位高，塞進格線會撐高整列。
- Prettier：無分號、單引號，提交前跑 `npm run format`。

---

## 九、目前的待辦與已知狀態

### 還沒做的

- **五個品項頁還是「待補」空頁**：C 型鋼、角鐵、H 型鋼、水槽鋼瓦、連接板。
  鋼板彎折與扁鐵已完成（兩者的圖面都已能帶進詢價單與 PDF）。
- `/login` 頁面檔案存在，但**沒有加進 `mainNavigation`**，所以導覽上進不去，
  只能直接打網址。
- `useAppNavigation.ts` 裡的 `/example/chart` 沒有對應的 page 檔案
  （`chart.js` 已在依賴裡，頁面沒建）。

### 沒進版控的檔案（待決定）

| 檔案                                | 原因                                                       |
| ----------------------------------- | ---------------------------------------------------------- |
| `視覺化參考.jpg`（根目錄）          | 參考圖，不是原始碼                                         |
| `public/images/steel/c-channel.svg` | **過時**，幾何跟 `components/UI/Steel/Icon.vue` 對不起來了 |
| `public/images/steel/preview.html`  | 同上，而且是臨時的預覽頁，沒有任何地方引用                 |

要清的話：刪掉 `public/images/steel/` 兩個檔，`.jpg` 加進 `.gitignore`。

> 注意：`c-channel` 這個**名字**在 `useAppNavigation.ts`、`pages/index.vue`、
> `RelatedList.vue`、`UI/Steel/Icon.vue` 都還在用，但那些指的是路由與 icon 名稱，
> 跟上面那個 svg 檔無關。刪檔案不會影響它們。

另外 `扁鐵-待確認事項.md` 已從磁碟刪除，但**刪除這件事還沒提交**，
所以 git 仍追蹤著它。要復原的話 `git checkout -- 扁鐵-待確認事項.md`。

### 這一輪還沒提交的東西

勾選模型 + 填寫流程抽元件那一整批都還在工作區，沒有 commit：

- 新檔（untracked）：`components/Product/FlatBarForm.vue`、`BendingForm.vue`、`StepNav.vue`
- 已改（modified）：`Checkbox.vue`、`useQuoteCart.ts`、`quote-cart.vue`、
  `pages/UI/forms.vue`、兩個品項頁、`main.css`、`memory.md`
- 已刪：`composables/useInstantQuote.ts`

### ⚠️ 匯出 PDF 還沒經過人眼驗證

程式面已確認：`npm run build` 通過、`npm run format:check` 全綠、
列印用的 CSS 與 `max-height: 55mm/70mm` 都確實編進 bundle。

但**列印預覽沒有人實際看過**（Claude 不跑 dev server）。要驗的話加四筆——
四刀第 1 種（兩張並排）、五刀第 1 種（單張、確認沒被拉伸）、盤形第 2 種、
扁鐵一筆——然後確認：

1. 頁數是 N+1（在驗 `overflow: hidden` 有沒有真的解掉）
2. **每頁的項目明細沒有被擠到第二頁**（在驗高度預算）
3. **清瀏覽器快取後再匯出一次** ← 圖片載入競態只有這條路徑會發作
4. 最後沒有多一張空白頁、表頭藍底有印出來

### ⚠️ 修改、勾選與送出詢價大部分還沒經過人眼驗證

`npm run build` 與 `format:check` 都過。

**已經看過畫面確認的**（只有這幾件）：

- 勾選欄的方框會正確變藍底白勾（就是第七節那個 Tailwind 坑，已修好）
- 勾一項時「已勾選 N 項」正確、三顆按鈕跟著亮、該列不淡化

下面這些**還是沒人看過**：

**填寫流程搬進元件是純重構，行為應該完全沒變**——這是最容易出錯的地方：

1. 扁鐵四步、彎版三步的解鎖與跳步跟改版前一樣
2. 換步驟時 `<main>` 有捲回頂端
3. 加入詢價後 1.5 秒提示 → 導回首頁

修改：

4. 點「修改」→ 全螢幕彈窗，**欄位是原本填的值**、可以直接點最後一步
5. **彈窗裡換步驟捲的是彈窗自己，不是背景那一頁**（這是最容易漏的坑）
6. 改完存檔 → 規格文字與圖面都更新、**項次與勾選狀態都沒變**
7. 不重傳檔案 → 參考圖檔記錄還在

勾選與送出詢價：

8. 品項頁按「送出詢價」→ 進詢價單，**原本的項目都還在但沒打勾**，只有新那項打勾
9. 按「加入詢價」的新項目**預設是勾選的**
10. 全選框：全勾打勾、勾一部分顯示「—」、都沒勾是空的
11. 取消勾選某列 → 該列變半透明
12. 一項都沒勾 → 三顆按鈕變灰 + 下方出現「請至少勾選一項」
13. **刪除一個已勾選的項目 → 全選框沒有卡在半勾**（在驗 `removeItem` 有沒有清 id）
14. 勾兩項匯出 PDF → 只有那兩項；勾一項 → **沒有封面頁**
15. 離開詢價單再回來 → **勾選狀態還在**
16. 手機寬度（375px）表格能橫向捲、勾選欄沒被擠掉、動作欄兩顆直排不爆版
